<?xml version="1.0" encoding="utf-8"?>
<!-- 方形组件添加页实时预览（Android 12+ previewLayout / 15+ setWidgetPreview），
     与真实 2×2 方形布局同构：封面（fitCenter 圆角对齐容器） + 歌名 + 小播放键。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/previewRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="10dp">

    <!-- 行1：封面（占上部权重，fitCenter 与真实 provider 渲染一致） -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="8dp"
        android:scaleType="fitCenter"
        android:src="@drawable/widget_preview_cover" />

    <!-- 行2：歌名(左) + 小播放键(右) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="弦予音乐"
            android:textColor="@color/widget_text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="middle" />

        <TextView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/widget_preview_dot" />
    </LinearLayout>
</LinearLayout>